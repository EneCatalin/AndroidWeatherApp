
    <PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

        <SwitchPreference
            android:defaultValue="true"
            android:key="notifications_key"
            android:title="Notifications" />

        <!-- NOTE: EditTextPreference accepts EditText attributes. -->
        <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
        <SwitchPreference
            android:defaultValue="true"
            android:key="dark_theme_key"
            android:title="Dark theme" />
        <SwitchPreference
            android:defaultValue="true"
            android:key="temperature_key"
            android:summary="Fahrenheit"
            android:title="Celsius" />
        <ListPreference
            android:defaultValue="3"
            android:entries="@array/entries_array"
            android:entryValues="@array/entries_values_array"
            android:key="GRR"
            android:summary="Select a language"
            android:title="Language" />



        <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
             dismiss it. -->
        <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->



    </PreferenceScreen>

